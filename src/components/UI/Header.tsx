import classes from "./Header.module.css";

const Header = () => {
	return (
		<div className={classes.header}>
			<header>
				<h1>Inspiration</h1>
			</header>
		</div>
	);
};

export default Header;
